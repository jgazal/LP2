/*4. O sistema de arquivos C ANSI utiliza arquivos texto e binário e dispõe de um
conjunto de funções de Entrada/Saída. Sobre ele é INCORRETO afirmar:
a) a função fopen é utilizada para abrir tanto arquivos texto quanto arquivos
binário.
b) as funções fread e fwrite não se aplicam a arquivos texto.
c) as funções fscanf e fprintf se aplicam apenas aos arquivos texto.
d) a função fclose é utilizada para fechar todo e qualquer arquivo previamente
aberto.
e) o modo a+b abre um arquivo binário para escrita.*/


A letra "e" está incorreta. O modo a+b Acrescenta dados ou cria um arquivo
binário para leitura e escrita. O mesmo que "a+" só que para arquivo binário.
